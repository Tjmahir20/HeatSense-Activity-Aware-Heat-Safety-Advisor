# Copy this file to ".env" in the same folder as app.py, then fill in values.
# Never commit ".env" or share your real API key.

# Required for OpenAI chat completions, embeddings (RAG), and evaluate_latency.py
OPENAI_API_KEY=sk-your-key-here
